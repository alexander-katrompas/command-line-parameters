
/* the following "if not defined" structure is needed so
 * the compiler does not read this file twice.
 */
#ifndef COMMANDLINE_MAIN_H
#define COMMANDLINE_MAIN_H

/* this is the library which allows printing with
 * the cout and endl objects.
 */
#include <iostream>

/* these statements bring the std namespace for cout and endl into
 * the program so the compiler can find them in the libraries.
 */
using std::cout;
using std::endl;

/* this ends the "if not defined" structure.
 * put ALL directives, prototypes inside the structure.
 */
#endif //COMMANDLINE_MAIN_H

